// 두수의 합, 차, 곱 나눗셈의 결과값을 출력하는 프로그램

#include <stdio.h>

int main(void) {
	int x = 20, y = 10;
	printf("두 수의 합: %d\n", x + y);
	printf("두 수의 차: %d\n", x - y);
	printf("두 수의 곱: %d\n", x * y);
	printf("두 수의 몫: %d\n", x / y);

	return 0;
}
